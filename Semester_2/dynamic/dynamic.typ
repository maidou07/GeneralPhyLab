// ================================================================
// Typst 实验报告模板 (已修正 context 错误)
// 移植自用户提供的 LaTeX 模板
// ================================================================

// --- 1. 文档与页面设置 ---
#set document(
  title: "动态法测定良导体的热导率（自主学习实验）",
  author: "[REDACTED]",
)
#let info-line(label, content) = {
  // 使用 block 创建一个带有底部边框的块，效果等同于下划线
  let underlined-content = block(
    width: 20%,              // 宽度撑满
    stroke: (bottom: 0.5pt),   // 只在底部画线
    inset: (bottom: 5pt),      // 文本与线的间距
    align(center, content)     // 内容水平居中
  )

  // 使用 grid 布局，让标签和下划线内容对齐
  grid(
    columns: (auto, 1fr),
    label,
    underlined-content
  )
}
#set page(
  paper: "a4",
  margin: (top: 1in, bottom: 1in, left: 1in, right: 1in),
  // 设置页眉
  header: [
    #grid(
      columns: (1fr, auto, 1fr),
      [#text(9pt)[实验名称：动态法测定良导体的热导率]],
      align(right)[#text(9pt)[普通物理实验报告]],
    )
    #v(-0.5em)
    #line(length: 100%, stroke: 0.5pt)
  ],
  // 设置页脚
  // -- 修正处：在 footer 的内容外包裹一个 context 块 --
  footer: context [
    #align(center)[
      #text(10pt)[
        第 #counter(page).display() 页 / 共 #counter(page).display(both: true).at(1) 页
      ]
    ]
  ],
)

// --- 2. 字体与段落设置 ---
#set text(
  font: "Songti SC", // 设置默认中文字体为宋体
  size: 12pt,
)
#set par(
  first-line-indent: 2em, // 设置段落首行缩进
)

// --- 3. 自定义函数 ---
// 定义一个 "question" 函数，用于设置问题的楷体样式
#let question(body) = {
  set text(font: "Kaiti SC") // 设置楷体
  body
}


// ================================================================
// 正文内容从这里开始
// ================================================================

// --- 报告标题区域 ---
#align(center)[
  #text(1.5em, weight: "bold")[动态法测定良导体的热导率]
  #v(0.5em)
  #text(1.2em, weight: "bold")[自主学习实验报告]
  #v(0.5em)
  #text(1em)[普通物理实验2]

  // -- 个人信息块 --
  // 使用 grid 来精确控制对齐和宽度
  #grid(
    columns: (auto, 3cm), // 第1列（标签）宽度自动，第2列（内容）固定为5cm
    column-gutter: 1em,   // 两列之间的间距
    row-gutter: 0.6em,
    // -- 以下是每一行的内容 -
    "姓名：", block(
      width: 100%, stroke: (bottom: 0.5pt), inset: (bottom: 4pt),
      align(center)[[REDACTED]]
    ),
    "学号：", block(
      width: 100%, stroke: (bottom: 0.5pt), inset: (bottom: 4pt),
      align(center)[[REDACTED]]
    ),
    "序号：", block(
      width: 100%, stroke: (bottom: 0.5pt), inset: (bottom: 4pt),
      align(center)[[REDACTED]]
    ),
  )
  #v(3em)
]
// --- 报告问题列表 ---
// 使用有序列表 (1., 2., ...) 来组织问题
#1. #question[简述本实验的测量原理，并从原理上思考在实验中需要保证的热学条件有哪些。]

*原理：*
本实验采用非稳态法 。在样品棒的一端施加简谐变化的温度边界条件 $T=T_0+T_m sin omega t$，这会在棒中激发出沿棒传播的“热波” 。热波波速 $v$ 与材料的热扩散率 $alpha$ 相关 ($v=sqrt(2 alpha omega) $)，而热扩散率 $ alpha$ 与热导率 $ kappa$ 相关 ($ alpha= kappa /(c rho)$)。通过测量波速 $v$，即可根据 $kappa=frac( upsilon^2 c rho,4 pi f)$ 计算出热导率。

*热学条件：*
- 保证热量仅沿棒轴线传输的一维热传导模型成立。
- 实现热端温度随时间按简谐形式变化的边界条件。
- 保持另一端（冷端）温度恒定为 $T_0$。

#2. #question[列举本实验是如何从实验系统设计、样品、实验操作等方面保证这些热学条件的。]

- *保证一维传播：*
  - 样品被制成圆棒状。
  - 棒的侧表面用绝热材料紧裹，减小对外散热，使热量只沿轴向传播。

- *实现简谐热端：*
  - 实验并不直接使用简谐热源，而是使用“开90s、关90s”的脉动热源热方波。\

    这种脉动温度波可分解为基波和多次谐波，在传播过程中，高次谐波因频率高而衰减极快。\

    传播一段距离后，仅剩下符合边界条件的基波（简谐波），从而实现简谐边界条件。\

  - 在不加热的半周期，热端不供冷水，加热半周期热端供冷水，以增大曲线幅度。

- *保持冷端恒温：*
  - 用冷却水持续冷却样品棒的尾端（冷端），使其温度保持恒定。

#3. #question[列举理想实验曲线应该满足的特征，在实验中如何判断曲线满足这些要求？不符合要求的曲线会给实验结果带来什么影响？]

*理想特征：*
- 曲线簇应为一组稳定、重复的$T-t$正弦曲线。
- 曲线簇中所有曲线的周期应相同。
- 沿传播方向，各曲线的振幅应依次衰减。
- 沿传播方向，各曲线的相位应依次延迟。

*如何判断：*
- 实验必须运行足够长的时间（如40-60分钟），使样品内温度达到动态稳定。
- 通过屏幕观察T-t曲线，判断其是否已停止漂移，形态是否稳定重复呈正弦规律，且周期等于热源周期。

*不符要求的影响：*
- 如果曲线不稳定或不是正弦波（高次谐波未衰减），将导致无法准确读取不同测量点之间的相位差（时间t值）。
- 时间延迟测量不准，会导致计算出的热波波速 $v$ 错误，进而导致热导率 $kappa$ 的计算结果错误。

#4. #question[本实验可否用来测量热的不良导体，为什么？]

不能。

*原因：* 
- *振幅衰减过快* 热的不良导体的热导率 $kappa$ 非常小，导致其热扩散率 $alpha= kappa/c rho$ 也非常小。根据热波方程的解，波的振幅随距离 $x$ 按 $e^(- sqrt(frac( omega,2a))x)$ 衰减。

  如果 $alpha$ 极小，衰减因子会非常大，导致热波在极短的距离内就迅速衰减至零。这样热波信号无法到达后续的传感器阵列，使得测量波速 $v$ 变得不可能。

- *热波波速过慢* $v = sqrt((2 alpha omega))$，$alpha$非常小，则波速非常小，达到动态稳定的时间极长，并且时间延迟过大，已经无法正确判断相位差是来源于哪一个周期，内部温度场混乱，无法测量。